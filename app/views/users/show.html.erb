 <div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">

       <%= render'/books/info', user: @user, book: @book %>
    </div>

    <h2>Books</h2>
    <table>
     <thead>
      <tr>
       <th>Title</th>
       <th>Opinion</th>
      </tr>
     </thead>

     <tbody>
      <% @books.each do |b| %>
      <tr>
       <td><%= b.image.tag user.get_profile_image(100,100) %></td>
       <td><%= b.link_to 'user:title', user_path(@user) %></td>
       <td><%= b.body %></td>
      </tr>
      <% end %>
     </tbody>
    </table>
  </div>
 </div>
